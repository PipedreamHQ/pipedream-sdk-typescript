// This file was auto-generated by Fern from our API Definition.

import * as Pipedream from "../../src/api/index";
import { PipedreamClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";
import { mockOAuth } from "./mockAuth";

describe("UsersClient", () => {
    test("deleteExternalUser (1)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new PipedreamClient({
            maxRetries: 0,
            projectId: "project_id",
            clientId: "client_id",
            clientSecret: "client_secret",
            projectEnvironment: "test",
            environment: server.baseUrl,
        });

        server
            .mockEndpoint()
            .delete("/v1/connect/project_id/users/external_user_id")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.users.deleteExternalUser("external_user_id");
        expect(response).toEqual(undefined);
    });

    test("deleteExternalUser (2)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new PipedreamClient({
            maxRetries: 0,
            projectId: "project_id",
            clientId: "client_id",
            clientSecret: "client_secret",
            projectEnvironment: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .delete("/v1/connect/project_id/users/external_user_id")
            .respondWith()
            .statusCode(429)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.users.deleteExternalUser("external_user_id");
        }).rejects.toThrow(Pipedream.TooManyRequestsError);
    });
});
