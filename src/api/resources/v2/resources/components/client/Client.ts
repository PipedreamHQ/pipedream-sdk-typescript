// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../../../core/headers.js";
import * as core from "../../../../../../core/index.js";
import * as environments from "../../../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../../../errors/index.js";
import * as serializers from "../../../../../../serialization/index.js";
import * as Pipedream from "../../../../../index.js";

export declare namespace ComponentsClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class ComponentsClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<ComponentsClient.Options>;

    constructor(options: ComponentsClient.Options) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * @param {Pipedream.v2.ComponentsListRequest} request
     * @param {ComponentsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Pipedream.BadRequestError}
     * @throws {@link Pipedream.UnauthorizedError}
     * @throws {@link Pipedream.InternalServerError}
     * @throws {@link Pipedream.BadGatewayError}
     * @throws {@link Pipedream.ServiceUnavailableError}
     *
     * @example
     *     await client.v2.components.list({
     *         q: "q",
     *         limit: 1000000,
     *         page: 1000000,
     *         componentType: "component_type",
     *         app: "app",
     *         registry: "registry"
     *     })
     */
    public list(
        request: Pipedream.v2.ComponentsListRequest = {},
        requestOptions?: ComponentsClient.RequestOptions,
    ): core.HttpResponsePromise<Pipedream.ComponentsListResponse> {
        return core.HttpResponsePromise.fromPromise(this.__list(request, requestOptions));
    }

    private async __list(
        request: Pipedream.v2.ComponentsListRequest = {},
        requestOptions?: ComponentsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Pipedream.ComponentsListResponse>> {
        const { q, limit, page, componentType, app, registry } = request;
        const _queryParams: Record<string, unknown> = {
            q,
            limit,
            page,
            component_type: componentType,
            app,
            registry,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "x-pd-environment": requestOptions?.projectEnvironment ?? this._options?.projectEnvironment,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PipedreamEnvironment.Prod,
                `v2/connect/${core.url.encodePathParam(this._options.projectId)}/components`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.ComponentsListResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Pipedream.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Pipedream.UnauthorizedError(_response.error.body, _response.rawResponse);
                case 500:
                    throw new Pipedream.InternalServerError(_response.error.body, _response.rawResponse);
                case 502:
                    throw new Pipedream.BadGatewayError(_response.error.body, _response.rawResponse);
                case 503:
                    throw new Pipedream.ServiceUnavailableError(
                        serializers.ErrorResponse.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PipedreamError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/v2/connect/{project_id}/components",
        );
    }

    /**
     * Mirrors `V1::Connect::ComponentsController#v2_facets`
     * (`PublishedComponentTypesenseSearch.facets`): returns
     * `{ data: { component_type: [...], app_name_slug: [...] } }`, scoped by the
     * caller's org via a per-request Typesense API key.
     *
     * @param {Pipedream.v2.ComponentsFacetsRequest} request
     * @param {ComponentsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Pipedream.BadRequestError}
     * @throws {@link Pipedream.UnauthorizedError}
     * @throws {@link Pipedream.InternalServerError}
     * @throws {@link Pipedream.BadGatewayError}
     * @throws {@link Pipedream.ServiceUnavailableError}
     *
     * @example
     *     await client.v2.components.facets({
     *         registry: "registry"
     *     })
     */
    public facets(
        request: Pipedream.v2.ComponentsFacetsRequest = {},
        requestOptions?: ComponentsClient.RequestOptions,
    ): core.HttpResponsePromise<Pipedream.ComponentsFacetsResponse> {
        return core.HttpResponsePromise.fromPromise(this.__facets(request, requestOptions));
    }

    private async __facets(
        request: Pipedream.v2.ComponentsFacetsRequest = {},
        requestOptions?: ComponentsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Pipedream.ComponentsFacetsResponse>> {
        const { registry } = request;
        const _queryParams: Record<string, unknown> = {
            registry,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "x-pd-environment": requestOptions?.projectEnvironment ?? this._options?.projectEnvironment,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PipedreamEnvironment.Prod,
                `v2/connect/${core.url.encodePathParam(this._options.projectId)}/components/facets`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.ComponentsFacetsResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Pipedream.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Pipedream.UnauthorizedError(_response.error.body, _response.rawResponse);
                case 500:
                    throw new Pipedream.InternalServerError(_response.error.body, _response.rawResponse);
                case 502:
                    throw new Pipedream.BadGatewayError(_response.error.body, _response.rawResponse);
                case 503:
                    throw new Pipedream.ServiceUnavailableError(
                        serializers.ErrorResponse.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PipedreamError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/v2/connect/{project_id}/components/facets",
        );
    }

    /**
     * Looks up a single published component by hashid (e.g. `pc_abc123`). IDs
     * that don't start with `pc_` return 404. Scoping is enforced via the same
     * per-request scoped Typesense API key as the listing, so private
     * components from other orgs are invisible (404, not 403, to avoid
     * confirming existence).
     *
     * @param {string} id - Published-component hashid (pc_...)
     * @param {Pipedream.v2.ComponentsRetrieveRequest} request
     * @param {ComponentsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Pipedream.BadRequestError}
     * @throws {@link Pipedream.UnauthorizedError}
     * @throws {@link Pipedream.NotFoundError}
     * @throws {@link Pipedream.InternalServerError}
     * @throws {@link Pipedream.BadGatewayError}
     * @throws {@link Pipedream.ServiceUnavailableError}
     *
     * @example
     *     await client.v2.components.retrieve("id", {
     *         registry: "registry"
     *     })
     */
    public retrieve(
        id: string,
        request: Pipedream.v2.ComponentsRetrieveRequest = {},
        requestOptions?: ComponentsClient.RequestOptions,
    ): core.HttpResponsePromise<Pipedream.ComponentResponse> {
        return core.HttpResponsePromise.fromPromise(this.__retrieve(id, request, requestOptions));
    }

    private async __retrieve(
        id: string,
        request: Pipedream.v2.ComponentsRetrieveRequest = {},
        requestOptions?: ComponentsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Pipedream.ComponentResponse>> {
        const { registry } = request;
        const _queryParams: Record<string, unknown> = {
            registry,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "x-pd-environment": requestOptions?.projectEnvironment ?? this._options?.projectEnvironment,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PipedreamEnvironment.Prod,
                `v2/connect/${core.url.encodePathParam(this._options.projectId)}/components/${core.url.encodePathParam(id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.ComponentResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Pipedream.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Pipedream.UnauthorizedError(_response.error.body, _response.rawResponse);
                case 404:
                    throw new Pipedream.NotFoundError(_response.error.body, _response.rawResponse);
                case 500:
                    throw new Pipedream.InternalServerError(_response.error.body, _response.rawResponse);
                case 502:
                    throw new Pipedream.BadGatewayError(_response.error.body, _response.rawResponse);
                case 503:
                    throw new Pipedream.ServiceUnavailableError(
                        serializers.ErrorResponse.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PipedreamError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/v2/connect/{project_id}/components/{id}",
        );
    }
}
